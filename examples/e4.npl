scope main {                               // definition of a scope of norms

    // some facts
    vl(20).

    // alice has 2 seconds to achieve a value of b = 0
    norm n1: vl(X) & X > 5
       -> obligation(alice,n1, b(0), `now`+`2 seconds`)
       if unfulfilled: s1(alice,X).

    sanction s1(A,X): c(Y) -> fine(A,X*Y).
       // value of c is provide by an external source

}
